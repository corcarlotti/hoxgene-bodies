#run blender script that creates json body shape dataset from hox genes and stores it in the datasets folder

#run script that trains a VAE from the json dataset made before, by first converting it into an appropriate array

#run the VAE with new hox gene inputs and convert the output array back into json files 

# open and render the json files in blender
